# 🩺 รายงานผลการจัดกลุ่มด้วยกฎ If-Else แบบง่าย (Interpretable Rule Report)
**โปรเจกต์:** การจำแนกประเภทมะเร็งด้วยกฎยีนเด่นที่มนุษย์อ่านเข้าใจง่าย  
**ยีนที่ใช้ในกฎ:**  
1. `ENSG00000164266_620977` (ยีนหลักตัวที่ 1)  
2. `ENSG00000019169_316124` (ยีนเสริมตัวที่ 2)  
3. `ENSG00000204305_741180` (ยีนเสริมตัวที่ 3)  

---

## 🧠 สรุปผลการประเมินบนชุดข้อมูลทั้งหมด (103 ตัวอย่าง)

| กฎ (Rule Model) | ค่าแม่นยำรวม (Accuracy) | ความไวในการตรวจมะเร็ง (Sensitivity) | ความจำเพาะกลุ่มปกติ (Specificity) | ทายผิดพลาด (Errors) |
| --- | --- | --- | --- | --- |
| **กฎ 1 ชั้น (ยีนเดี่ยว)** | **92.23%** | 89.29% (50/56) | 95.74% (45/47) | 8 ตัวอย่าง |
| **กฎ 2 ชั้น (3 ยีนซ้อน)** | **98.06%** | **98.21% (55/56)** | **97.87% (46/47)** | **2 ตัวอย่าง** |

---

## 💻 โค้ดภาษา Python สำหรับนำไปใช้งานจริง (Production-Ready)

คุณสามารถคัดลอกฟังก์ชัน Python ด้านล่างนี้ไปใช้ทำนายผลได้ทันทีครับ:

```python
def classify_cancer_rule_1(exon_164266):
    """
    กฎ If-Else ชั้นเดียว (Single-Gene Classifer)
    ความแม่นยำบนชุดข้อมูลนี้: 92.23%
    """
    if exon_164266 > 2.75:
        return "Cancer"   # มะเร็ง
    else:
        return "Healthy"  # ปกติ

def classify_cancer_rule_2(exon_164266, exon_019169, exon_204305):
    """
    กฎ If-Else ซ้อนสองชั้น (Nested 3-Gene Classifier)
    ความแม่นยำบนชุดข้อมูลนี้: 98.06%
    """
    if exon_164266 <= 2.75:
        # เช็กระดับยีนเสริมตัวแรก
        if exon_019169 <= 2.71:
            return "Cancer"
        else:
            return "Healthy"
    else: # ถ้า exon_164266 > 2.75
        # เช็กระดับยีนเสริมตัวที่สอง
        if exon_204305 <= 5.77:
            return "Cancer"
        else:
            return "Healthy"
```

---

## 📊 ตารางประเมินผลความผิดพลาด (Confusion Matrices)

### 1. กฎ 1 ชั้น (ยีนเดี่ยว): ผิดพลาด 8 ตัวอย่าง
* ทายว่าปกติแต่จริง ๆ เป็นมะเร็ง (False Negative): **6 ราย**
* ทายว่าเป็นมะเร็งแต่จริง ๆ ปกติ (False Positive): **2 ราย**

| | ทายว่า ปกติ (Predicted Healthy) | ทายว่า มะเร็ง (Predicted Cancer) |
| :--- | :---: | :---: |
| **ความจริง ปกติ (Actual Healthy)** | **45** (ทายถูก) | **2** (ทายผิด) |
| **ความจริง มะเร็ง (Actual Cancer)** | **6** (ทายผิด) | **50** (ทายถูก) |

---

### 2. กฎ 2 ชั้น (3 ยีน): ผิดพลาดเพียง 2 ตัวอย่าง!
* ทายว่าปกติแต่จริง ๆ เป็นมะเร็ง (False Negative): **1 ราย**
* ทายว่าเป็นมะเร็งแต่จริง ๆ ปกติ (False Positive): **1 ราย**

| | ทายว่า ปกติ (Predicted Healthy) | ทายว่า มะเร็ง (Predicted Cancer) |
| :--- | :---: | :---: |
| **ความจริง ปกติ (Actual Healthy)** | **46** (ทายถูก) | **1** (ทายผิด) |
| **ความจริง มะเร็ง (Actual Cancer)** | **1** (ทายผิด) | **55** (ทายถูก) |

---

## 🔍 บทสังเกตที่สำคัญทางวิชาการ (Academic Insights)

1. **ทำไมรันตรงนี้ได้ 98% แต่ตอนทดสอบ Cross-Validation (LOO-CV) ได้ 89.32%?**
   * **98.06% (Self-Evaluation Accuracy):** เกิดจากการที่เรากำหนดขีดเส้นแบ่ง (Thresholds เช่น `2.75`, `2.71`, `5.77`) โดยใช้ข้อมูลทั้งหมด 103 คนพร้อมกัน ซึ่งจะจำแนกกลุ่มข้อมูลชุดนี้ได้ดีที่สุด
   * **89.32% (LOO-CV Accuracy):** เกิดจากการจำลองสถานการณ์จริงเมื่อคนไข้คนใหม่นอกกลุ่มทดสอบเข้ามา (โมเดลต้องหาเส้นแบ่งใหม่จากข้อมูลคนไข้ 102 คนที่เหลือ แล้วไปทำนายคนที่โดนดึงออก) ซึ่งค่านี้คือ **"ความเที่ยงตรงที่แท้จริง (Generalizability)"** ของการนำกฎนี้ไปใช้กับโรงพยาบาลหรือคนไข้รายใหม่อื่นๆ
2. **ความเชื่อมโยงกับ PCA:**
   * สอดคล้องกับที่คุณสังเกต! เนื่องจากบนแกน PC1 ข้อมูลหลังแก้ Batch effect แยกจากกันเกือบเด็ดขาดอยู่แล้ว ยีนเด่นที่เป็นตัวขับเคลื่อนแกน PC1 (นั่นคือ `ENSG00000164266_620977`) จึงสามารถสร้างระดับกฎ If-Else ชั้นเดียวที่ทำความแม่นยำทะลุ 92% ได้ทันที
